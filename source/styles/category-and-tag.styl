span.category-tree
    a, /span.tag a
        all: unset

        &:hover
            cursor: pointer
        &::after
            display: none

span.category-separator
    display: inline-block;
    transform: rotate(-0.02turn);
