<% const categories =
    page.path.split("/").slice(config.category_dir.split("/").length,
    page.path.split("/").length - 1) %>
<main>
    <h1>
        <span class="category-tree">
        <% this_category_tree(categories, site.categories, categories.length).forEach((category) => { %>
            <span><%= category.name %></span>
            <span class="separator">/</span>
        <% }) %>
        </span>
    </h1>
    <ul class="theme-list posts-list">
        <% page.posts.forEach((post) => { %>
            <%- partial(
                "partials/post-li",
                { post, categories, depth: Infinity }
            ) %>
        <% }) %>
    </ul>
</main>
