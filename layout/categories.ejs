<main>
    <h1><%= __("titles.categories") %></h1>
    <ul class="theme-list">
        <% page.posts.forEach((root) => { %>
            <%- list_child_categories(root) %>
        <% }) %>
    </ul>
</main>
